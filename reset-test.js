describe("Once the reset button is clicked the displayed text should be cleared", function(){
  it("The reset button function should clear the display text field once clicked", function(){
     var reset= FactoryReset();

     assert.equal()

  });
});
